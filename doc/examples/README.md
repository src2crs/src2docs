# Examples and Test Data

This module contains examples for source files that can be fed into the library
as well as the corresponding expected output/result files.

These are meant as documentation and can be used as test data for integration tests.
