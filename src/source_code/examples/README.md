# Examples and Test Data

This module contains examples for source files that can be parsed by the library.

The examples are organized in subdirectories based on their
programming language.
For each source file, there is a corresponding constructor
in `SourceCode` and there are tests to check that these files
are parsed correctly.
